# The payments service: whether money moved.
#
# Holds the only connection to the bank.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payments
  namespace: tickets
  labels: {app: payments}
spec:
  replicas: 1
  selector:
    matchLabels: {app: payments}
  template:
    metadata:
      labels: {app: payments}
    spec:
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: payments
          image: ghcr.io/slash3b/tickets-payments:latest   # CI rewrites this
          ports:
            - {name: http, containerPort: 8080}
            - {name: grpc, containerPort: 9090}
          env:
            - {name: PORT, value: "8080"}
            - {name: GRPC_PORT, value: "9090"}
            - {name: KAFKA_BROKERS, value: "tickets-kafka-kafka-bootstrap.data.svc.cluster.local:9092"}
            - {name: OTEL_EXPORTER_OTLP_ENDPOINT, value: "signoz-otel-collector.signoz.svc.cluster.local:4318"}
            - name: POD_NAME
              valueFrom:
                fieldRef: {fieldPath: metadata.name}
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef: {fieldPath: metadata.namespace}
            - name: NODE_NAME
              valueFrom:
                fieldRef: {fieldPath: spec.nodeName}
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: "deployment.environment.name=homelab,k8s.cluster.name=homelab,k8s.pod.name=$(POD_NAME),k8s.namespace.name=$(POD_NAMESPACE),k8s.node.name=$(NODE_NAME)"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef: {name: tickets-pg-app, key: uri}
            - {name: BANK_URL, value: "http://bank.bank.svc.cluster.local"}
          resources:
            requests: {cpu: 20m, memory: 56Mi}
            limits:   {memory: 168Mi}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: {drop: [ALL]}
          livenessProbe:
            httpGet: {path: /livez, port: http}
            periodSeconds: 10
          readinessProbe:
            # Readiness pings the database, so a service that cannot reach
            # Postgres leaves the rotation instead of failing every RPC.
            httpGet: {path: /readyz, port: http}
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: payments
  namespace: tickets
spec:
  selector: {app: payments}
  ports:
    - {name: http, port: 80,   targetPort: http}
    - {name: grpc, port: 9090, targetPort: grpc}
