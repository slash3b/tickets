apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: hello
resources:
- namespace.yaml
- deployment.yaml
- httproute.yaml
# CI rewrites newTag here on every push to main. This line is the record of what
# is actually deployed.
images:
- name: ghcr.io/slash3b/tickets-hello
  newTag: d1ca7bbbd744bae5d7e467bedf673f77a8e98c48
